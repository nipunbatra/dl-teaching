# Retired Marp course (archive)

This folder holds the **previous** version of the ES 667 course, authored as
[Marp](https://marp.app/) slide decks in Markdown (`L01–L26*.md`, plus older
decks in `archive/`). It is kept for reference only.

**The current course is authored entirely in Typst** — see `lecture*/L*.typ`
and the [Slides](../slides.qmd) page. Do **not** add new material here; this
directory is not built or deployed (the `_` prefix makes Quarto ignore it).

To view an archived deck: `npx @marp-team/marp-cli <file>.md --html --allow-local-files`.
